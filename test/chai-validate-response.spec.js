"use strict";

let chai = require("chai"),
    chaiValidateResponse = require("../lib/chai-validate-response");

chai.use(chaiValidateResponse);

describe("chai-validate-response", () => {
    describe("expect(res).to.be.a.validResponse(schema)", () => {

    });
});